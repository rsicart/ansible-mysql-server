Ansible role to install a Mysql server.
=======================================

This role is intended to be used with Debian based distributions.

* Installs mysql packages
* Set root's password
* Creates ~root/.my.cnf

Don't forget to override defaults vars.
